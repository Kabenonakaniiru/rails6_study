= form_with model:  Mission.new, url: { action: "create" }, local: true do |f|
  = render 'form', f: f
  = f.submit '登録', class: 'btn btn-secondary'
  = link_to '戻る', admins_missions_path, class: 'btn btn-secondary'